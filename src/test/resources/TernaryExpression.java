public class TernaryExpression {
    public static void main(String[] args) {
        String value = "Hello";
        System.out.println(value.charAt(0) == 'H' ? value.toLowerCase() : value.toUpperCase());
    }
}
